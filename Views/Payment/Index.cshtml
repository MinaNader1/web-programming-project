@model IEnumerable<depi_real_state_management_system.Models.Payment>

@{
    ViewData["Title"] = "Payments";
}

<h1>Payments</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New Payment</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Tenant</th>
            <th>Lease ID</th>
            <th>Amount Paid</th>
            <th>Payment Date</th>
            <th>Status</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var payment in Model)
        {
            <tr>
                <td>@payment.Tenant.UserName</td>
                <td>@payment.LeaseID</td>
                <td>@payment.AmountPaid</td>
                <td>@payment.PaymentDate.ToShortDateString()</td>
                <td>@payment.PaymentStatus</td>
                <td>
                    <a asp-action="Details" asp-route-id="@payment.PaymentID" class="btn btn-info">Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>
